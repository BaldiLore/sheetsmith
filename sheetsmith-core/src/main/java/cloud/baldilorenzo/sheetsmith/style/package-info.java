/**
 * Style enums used as attribute values of the sheetsmith annotations.
 * <p>
 * Enums that mirror an Apache POI enum contain the constant {@code INHERIT} plus one constant for each POI
 * constant, with identical names. {@code INHERIT} means that the attribute is not set at that level and
 * keeps the value of the level below.
 */
package cloud.baldilorenzo.sheetsmith.style;
